{
  "name": "@leolabs/polymarket-executor",
  "version": "0.7.0-migration.0",
  "private": true,
  "description": "Opt-in Polymarket order execution module for polymarket-toolkit. Uses the unified @polymarket/client SDK; isolated from the zero-dependency read-only core.",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "tsc --noEmit",
    "dry-run": "node --experimental-strip-types examples/dry-run.ts"
  },
  "dependencies": {
    "@polymarket/client": "^0.9.0",
    "ethers-v5": "npm:ethers@^5.8.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=24"
  }
}
